apiVersion: v1
kind: Service
metadata:
  name: {{ include "multiplication-app.fullname" . }}
  labels:
    {{- include "multiplication-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multiplication-app.selectorLabels" . | nindent 4 }}
